interface:
  display_name: "Vibe Control"
  short_description: "用持续仪表台、自动协作和独立审计推进可观察、可验证的软件开发"
  default_prompt: "Use $vibe-control 0.4.0 to initialize the local progress dashboard, establish the Schema 4.0 control plane, and automatically advance through TEAM → SUBAGENT → SERIAL to the next real Owner review while preserving deterministic evidence boundaries and plain-language reporting."

policy:
  allow_implicit_invocation: true
