interface:
  display_name: "Vibe Control"
  short_description: "用检查点、目标缰绳与跨宿主协调兼容层建立可验证控制面"
  default_prompt: "Use $vibe-control to derive and lock key objectives, establish a Schema 3.2 control plane, confirm preset acceptance checkpoints, resolve CODEX_THREADS/SUBAGENTS/SERIAL from actual host capabilities, and stop audits when their bounded contract closes."

policy:
  allow_implicit_invocation: true
